//this file is created for the purpose of storing all the components created and saved in one file, so when calling them in App.jsx we will only need to import this one file and all components will be available.

import InputBox from "./inputBox";

export {InputBox}